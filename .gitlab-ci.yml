image: dettmering/hugo-build:0.53

variables:
    url: https://netsplit.uk/

before_script:
    - echo "Test\n";

stages:
    - generate
    - deploy

generate:
    stage: generate
    script:
        - echo "Test generate\n"
        - hugo version

deploy:
    stage: deploy
    script:
        - echo "Test deploy\n"
        - hugo version
